name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  packages: write

jobs:
  release:
    name: Create GitHub Release with Changelog
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: apps/frontend/package-lock.json

      # ── Verify tests pass before cutting release ──────────────────────
      - name: Install apps/backend deps
        run: |
          python -m pip install --upgrade pip
          pip install -e "./apps/backend[dev]"

      - name: Backend tests
        working-directory: apps/backend
        run: pytest tests/ -q

      - name: Install apps/frontend deps
        working-directory: apps/frontend
        run: npm ci

      - name: Frontend tests
        working-directory: apps/frontend
        run: npm run test:ci

      # ── Native Linux binary ────────────────────────────────────────────
      - name: Build native Linux binary
        run: |
          pip install pyinstaller
          pyinstaller --onefile --noconsole \
            --name "circuit-breaker-${{ github.ref_name }}-linux-amd64" \
            apps/backend/src/app/main.py

      # ── GitHub Release ─────────────────────────────────────────────────
      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            dist/circuit-breaker-${{ github.ref_name }}-linux-amd64
