name: Dev CI

on:
  push:
    branches: [dev]

jobs:
  # ── Lint ─────────────────────────────────────────────────────────────────────
  lint:
    name: Lint
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - uses: actions/setup-node@v5
        with:
          node-version: "20"

      - name: Install Python deps
        run: |
          python3.12 -m venv .venv
          .venv/bin/pip install --upgrade pip
          .venv/bin/pip install -e "apps/backend/[dev]"

      - name: Alembic revision graph (single head)
        env:
          CB_DB_URL: postgresql://cb_ci:cb_ci@127.0.0.1:5432/cb_ci
        run: |
          cd apps/backend
          PYTHONPATH=src ../../.venv/bin/python -c "
          from alembic.config import Config
          from alembic.script import ScriptDirectory
          cfg = Config('alembic.ini')
          heads = ScriptDirectory.from_config(cfg).get_heads()
          assert len(heads) == 1, f'expected 1 Alembic head, got: {heads!r}'
          print('Alembic head:', heads[0])
          "

      - name: Install frontend deps
        run: cd apps/frontend && npm ci

      - name: Ruff check
        run: cd apps/backend && ../../.venv/bin/ruff check src/app

      - name: Mypy
        run: cd apps/backend && PYTHONPATH=src ../../.venv/bin/mypy src/app

      - name: ESLint
        run: cd apps/frontend && npm run lint

  # ── Tests ─────────────────────────────────────────────────────────────────────
  backend-tests:
    name: Backend tests
    runs-on: ubuntu-22.04
    # Docker is required, not optional: tests/conftest.py boots a TimescaleDB
    # testcontainer and sets CB_DB_URL from it BEFORE any app module is
    # imported, so there is no sqlite-only mode to fall back to. ubuntu-22.04
    # runners ship with Docker already running.
    #
    # Added 2026-08-09 (F-9). Until then no workflow ran backend pytest at all
    # — the whole 2146-test suite was enforced locally only, so nothing found
    # during user testing could be kept fixed. The blocker was the coverage
    # gate in apps/backend/pyproject.toml, which demanded 60% against a real
    # 55.42%; it is now ratcheted to the measured number, so this job's exit
    # code means what it says.
    #
    # The 35+ minute docker e2e suite (apps/agent/e2e) is deliberately NOT run
    # here and remains a local/manual gate.
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install Python deps
        run: |
          python3.12 -m venv .venv
          .venv/bin/pip install --upgrade pip
          .venv/bin/pip install -e "apps/backend/[dev]"

      - name: Backend tests
        run: cd apps/backend && PYTHONPATH=src ../../.venv/bin/pytest

  test:
    name: Test
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: "20"

      - uses: actions/setup-go@v5
        with:
          go-version: "1.22"
          cache-dependency-path: apps/agent/go.sum

      - name: Install frontend deps
        run: cd apps/frontend && npm ci

      - name: Frontend tests
        run: cd apps/frontend && npm test

      # -race is mandatory: the agent's daemon-startup ordering guarantees are
      # only observable under the race detector (see apps/agent/Makefile).
      - name: Agent tests
        run: cd apps/agent && make test

      - name: Agent vet
        run: cd apps/agent && go vet ./...

  # ── Security Gate ─────────────────────────────────────────────────────────────
  security-gate:
    name: Security Gate
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install scan tools
        run: |
          python3.12 -m venv .venv
          .venv/bin/pip install --upgrade pip
          .venv/bin/pip install bandit semgrep

      - name: Run security gate (fails on HIGH/CRIT)
        run: ./scripts/security_scan.sh --gate

      - name: Upload scan report
        uses: actions/upload-artifact@v5
        if: always()
        with:
          name: security-scan-report
          path: security_scan_report.md
          retention-days: 30

  # ── Native Build (amd64 only) ─────────────────────────────────────────────────
  build-native:
    name: Build Native (amd64)
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - uses: actions/setup-node@v5
        with:
          node-version: "20"

      - name: Derive version
        id: ver
        run: echo "version=dev-$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"

      - name: Install build deps
        run: bash scripts/install-build-deps.sh

      - name: Install Python deps
        run: |
          python3.12 -m venv .venv
          .venv/bin/pip install --upgrade pip
          .venv/bin/pip install -e "apps/backend/[dev]"

      - name: Build frontend
        run: cd apps/frontend && npm ci && npm run build

      - name: Build packages
        env:
          CB_VERSION: ${{ steps.ver.outputs.version }}
        run: |
          .venv/bin/python scripts/build_native_release.py \
            --version "$CB_VERSION" \
            --clean

      - name: List artifacts
        run: ls -lh dist/native/

      - name: Upload artifacts
        uses: actions/upload-artifact@v5
        with:
          name: dev-packages-amd64
          path: dist/native/
          if-no-files-found: error
          retention-days: 3

  # ── Docker Smoke Build ────────────────────────────────────────────────────────
  build-docker:
    name: Build Docker (smoke test)
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v5

      - name: Derive version
        id: ver
        run: echo "version=dev-$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"

      - name: Build Docker image
        env:
          CB_VERSION: ${{ steps.ver.outputs.version }}
        run: |
          docker build -f Dockerfile.mono \
            -t "circuitbreaker:${CB_VERSION}" \
            .

      - name: Verify agent binaries are baked into the image
        env:
          CB_VERSION: ${{ steps.ver.outputs.version }}
        run: |
          docker run --rm --entrypoint sh "circuitbreaker:${CB_VERSION}" \
            -c 'test -s /opt/circuitbreaker/agent-binaries/manifest.json && ls /opt/circuitbreaker/agent-binaries'
