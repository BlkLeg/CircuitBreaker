# VS Code test environment for backend pytest discovery
# Update these values if your local services use different ports/credentials.

CB_DB_URL=postgresql://breaker:breaker@localhost:5432/circuitbreaker
CB_REDIS_URL=redis://localhost:6379/0
CB_NATS_URL=nats://localhost:4222

# Keep test mode explicit for fixtures and faster startup paths.
CB_TESTING=1
