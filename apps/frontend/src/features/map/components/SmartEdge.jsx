/**
 * SmartEdge — re-exports CustomEdge for backward compatibility.
 * All SmartEdge features (draggable bend handles, control points)
 * are preserved in CustomEdge alongside v2 connection-type styling.
 */
export { default } from './CustomEdge';
