# Handoff — bug-bounty remediation, 2026-08-26 evening

Written for whoever picks this up next. The authoritative record is
[`bug-bounty-2026-08-26.md`](./bug-bounty-2026-08-26.md); this file is only the
operating instructions that do not belong in it.

## Where things stand

* `git log -1` → `940e74ad` (the report update). The commit before it, `2d6368ac`, is the
  clean base every agent worked from.
* **The working tree is dirty on purpose.** ~60 files carry uncommitted fixes for 16 of
  the original 35 findings plus B36. Nothing is closed until it is committed.
* One workflow (`wf_e28a8b43-57e`, 8 fix agents + 8 refute agents) was **still running**
  when this was written, covering B11, B12 (Python half), B13, B14, B22, B23, B24, B25,
  B26, B27, B28, B30, B34. Its results land in
  `~/.claude/projects/-home-shawnji-Documents-Projects-CircuitBreaker/40d179d0-3346-42af-9f26-d5345a740a65/subagents/workflows/wf_e28a8b43-57e/journal.jsonl`
  — one `{"type":"result"}` line per agent. Read that before assuming anything about
  those findings.

## Do these in order

1. **Read the journal** for `wf_e28a8b43-57e` and fold its results into the report the
   same way the earlier waves were folded in: status rows, plus any regression its
   reviewers found added to the R-table, plus any new bug added to the B36+ table.

2. **Fix the thirteen regressions** in the report's "Regressions this remediation
   introduced" table. R1–R4 and R9 are release blockers — an uninstaller that aborts after
   confirming an irreversible prompt (R4) and an installer that dies with no output (R9)
   are worse than the defects they replaced. R13 names two tests that pin nothing; fix the
   tests, do not just delete them.

3. **Author the one Alembic revision.** Head is `7d2e4a81c6f3`. It needs to cover
   whatever B13 (composite index on `listener_events (ip_address, service_type, seen_at)`),
   B14 (`AppSettings.audit_chain_checkpoint_hash`) and B34 (data repair for
   double-encoded `properties_json`) reported under `migration_needed`. Agents were told
   not to write revisions precisely so this would be one revision and not three heads.
   **Backend tests build schema from `models.Base.metadata.create_all`, not Alembic**, so a
   missing revision is invisible to the suite and only shows up as a broken upgrade.

4. **Wire the `listener_events` retention purge into `main.py`.** The B13 agent was
   forbidden `main.py` (to avoid colliding with B09) and reported the registration snippet
   under `needs_other_files`.

5. **Fix B37** — the conftest `CB_DATA_DIR` redirect. Deferred only because every agent
   was running tests against `conftest.py` at the time. Safe once the tree is quiet.

6. **Verify properly.** Both of these must be green, and the backend one must be run
   *without* narrowing to a subset, because B36 was only visible in a full ordered run:

   ```
   pytest tests -q                                    # repo root, ~8s, was 281 passed
   cd apps/backend && CB_TEST_DB_URL=... CB_ALLOW_DEGRADED_DEPENDENCIES=true \
     CB_ALLOW_DIRECT_EGRESS=true PYTHONPATH=src pytest tests -q -p no:randomly
   ```

   The backend suite spins up its own Postgres testcontainer per session, so parallel runs
   are isolated — that is what made the fan-out safe. Its `addopts` carries `-x`, so a
   green tail is not proof the whole suite ran; check the count.

7. **Version bump.** `VERSION` reads `1.0.0-rc.4`; the last stable tag is `v0.3.4`. The
   decision on record is **0.4.0** — a minor bump from 0.3.4, short of 1.0, honest about
   scope, because this carries real behaviour changes (uninstall defaults to retaining
   data, install fails closed on a missing checksum, upgrade aborts instead of lying about
   a backup). `tests/build/test_version_parity.py` (GOV-09) requires `VERSION`,
   `package.json` and `apps/frontend/package.json` to move together, and
   `scripts/check_version_parity.py` also checks doc versions.

8. **Commit.** The goal is a completely clean tree. Commit per finding-cluster with the
   message style this repo uses (`fix(security): <what broke, in a sentence>`), and then
   go back and put the commit hashes into the report's status rows — a finding is closed
   by naming the commit, not by an adjective.

## Things that will bite you

* **A pre-commit hook runs `ruff check --fix`, `ruff format` and eslint/prettier on staged
  files, and reverts the whole commit if any fail.** Run
  `.venv/bin/ruff check apps/backend/src apps/backend/tests` before committing.
* `git status` will look alarming. It is supposed to.
* Agents were each given a disjoint file-ownership list; that is the only thing that kept
  them from colliding. Keep doing it if you fan out again.
* The report's own warning still applies to the thirteen unfixed findings: the fix text
  under each is the hunt's, verbatim, unverified, with line numbers as of `f483bcb3`.
  Confirm against the tree before acting. One in six examined closely (B04) was wrong
  about its own reachability, and in this pass two more (B05, B09) reached the right
  conclusion by the wrong stated mechanism.
