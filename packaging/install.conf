# Distro-package layout consumed by /usr/local/bin/cb.
# Prefer /etc/circuit-breaker/install-identity.json (written by postinstall).
CB_MODE=binary
CB_DATA_DIR=/var/lib/circuit-breaker
CB_BINARY=/usr/local/bin/circuit-breaker
CB_BINARY_ENV_FILE=/etc/circuit-breaker/circuit-breaker.env
CB_PORT=8000
